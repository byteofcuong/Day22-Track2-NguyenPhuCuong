# ══════════════════════════════════════════════════════════════════════════
#  Day 22 Lab — Dependencies
#
#  ⚠️  CÁC RÀNG BUỘC TRẦN (<) LÀ BẮT BUỘC, KHÔNG PHẢI CHO ĐẸP.
#  Bản gốc chỉ ghi `langchain>=0.3.0` nên pip kéo về langchain 1.x, và
#  `ragas 0.4.3` chết ngay lúc import vì langchain-community 0.4.x đã xoá
#  module `langchain_community.chat_models.vertexai` mà ragas còn import.
#  Giữ nguyên dòng 0.3.x là điều kiện để cả 4 bước chạy được cùng nhau.
#
#  Bộ version dưới đây đã được xác minh chạy thật trên Python 3.13.7 / Windows.
# ══════════════════════════════════════════════════════════════════════════

# ── LangChain core (ghim dòng 0.3.x — xem ghi chú trên) ──────────────
langchain>=0.3,<0.4
langchain-core>=0.3,<0.4
langchain-community>=0.3,<0.4
langchain-text-splitters>=0.3,<0.4

# ── LLM Providers (cài tất cả để hỗ trợ đa provider) ─────────────
langchain-openai>=0.3,<0.4          # OpenAI + OpenRouter
langchain-anthropic>=0.3,<0.4       # Anthropic Claude
langchain-google-genai>=2.0,<3.0    # Google Gemini
langchain-ollama>=0.2,<0.4          # Ollama (local)

# ── LangSmith ─────────────────────────────────────────────────────
langsmith>=0.2.0

# ── Vector store ──────────────────────────────────────────────────
faiss-cpu>=1.7.0

# ── RAG Evaluation ────────────────────────────────────────────────
ragas>=0.4.0
datasets>=2.0.0                  # required by RAGAS internally
numpy>=1.25.0

# ── Guardrails AI ─────────────────────────────────────────────────
# guardrails-ai 0.11 khai báo cần langchain-core>=1.0 nên pip sẽ in cảnh báo
# xung đột với dòng 0.3.x ở trên. Cảnh báo này VÔ HẠI: guardrails chỉ dùng
# langchain-core cho type hint và integration tuỳ chọn, không dùng ở đường
# chạy của Bước 4 — đã kiểm chứng import + chạy Guard thành công.
# (Bản 0.6.x tương thích metadata nhưng lại đòi openai<2 → vỡ chỗ khác.)
guardrails-ai>=0.5.0

# ── Utilities ─────────────────────────────────────────────────────
python-dotenv>=1.0.0
openai>=1.0.0                    # OpenAI SDK (used by langchain-openai)
tiktoken>=0.5.0                  # token counting
